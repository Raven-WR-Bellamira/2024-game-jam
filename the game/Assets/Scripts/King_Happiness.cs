using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class King_Happiness : MonoBehaviour
{
    //This script is for the kings joy meter,if the joy meter gets to low player dies
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
